#Looping over a collection.
'''
colors = ['red', 'green', 'blue', 'yellow']

for i in range(len(colors)):
    print(colors[i])
'''

colors = ['red', 'green', 'blue', 'yellow']

for color in colors:
    print(color)
